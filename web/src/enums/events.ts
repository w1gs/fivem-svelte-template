export enum Receive {
    visible = 'ncontrolui:visible',
    configData = 'ncontrolui:configData',
}

export enum Send {
    close = 'ncontrolui:close',
    clearArea = 'ncontrolui:clearArea',
    saveSettings = 'ncontrolui:saveSettings'
}
